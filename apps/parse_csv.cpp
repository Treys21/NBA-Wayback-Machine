//
// Created by Anik on 5/6/2020.
//

#include "parse_csv.h"
#include <vector>

using namespace std;

namespace myapp {

    /*
    int[] find_player(string name) {
        if (csv.mmap("nba-data-historical.csv")) {
            for (auto row: csv) {
                for (auto cell: row) {
                    string value;
                    cell.read_value(value);
                    if (name.compare(value) != 0) {
                        int year;
                        row.begin();

                    }
                        row.
                }
            }
        }
    }

    double[] find_similarities(int row) {

    }

    std::string print_stats(int row) {

    }
     */
}
